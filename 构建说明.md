# 灌区配水管理系统 - 构建与运行说明

## 快速运行

### 方式一：直接运行（推荐）
双击 `run-app.bat` 即可启动应用。

### 方式二：命令行运行
```bash
# 1. 安装依赖（首次运行）
npm install

# 2. 开发模式运行
npm run dev

# 3. 新开终端，启动 Electron
npx tsc -p electron/tsconfig.json
set NODE_ENV=development
npx electron .
```

## 打包发布

### 前置条件
- Windows 系统需要以**管理员身份**运行命令行（用于创建符号链接）
- 或者使用以下免安装打包方式

### 方式一：生成便携版（无需安装）
```bash
# 1. 构建前端
npx vite build

# 2. 编译 Electron
npx tsc -p electron/tsconfig.json

# 3. 使用 electron-packager 打包（推荐）
npx electron-packager . "灌区配水管理系统" --platform=win32 --arch=x64 --out=release --overwrite --ignore="node_modules/electron-builder"
```

### 方式二：生成安装包（需要管理员权限）
```bash
# 以管理员身份运行 PowerShell 或 CMD
npm run build:installer
```

### 构建产物说明
- `dist/` - 前端构建产物
- `dist-electron/` - Electron 主进程编译产物
- `release/` - 打包后的可执行文件

## 常见问题

### Q: electron-builder 打包时报符号链接错误？
A: 这是 Windows 权限问题，请以管理员身份运行命令行，或使用 electron-packager 替代。

### Q: 应用启动后显示空白？
A: 请确保已运行 `npx vite build` 和 `npx tsc -p electron/tsconfig.json`。

### Q: 如何在开发模式下调试？
A: 运行 `npm run dev` 启动 Vite 开发服务器，然后运行 Electron，会自动打开开发者工具。
